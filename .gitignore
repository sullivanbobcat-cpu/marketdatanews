node_modules/
netlify/functions/node_modules/
package-lock.json

# SEO audit reports — internal only, never commit
_reports/

# SQLite databases — binary, use JSON export instead
data/*.db

# Generated data exports — populated by scripts, not committed
data/port-fee-series.json
